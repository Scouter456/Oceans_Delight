modLoader="javafml" #mandatory
loaderVersion="[40,)"
license="MIT"
issueTrackerURL="https://github.com/Scouter456/Nether_Depths_Upgrade/issues"
[[mods]]
modId="netherdepthsupgrade" #mandatory
version="${file.jarVersion}" #mandatory
displayName="Ocean's Delight"
#updateJSONURL="https://change.me.example.invalid/updates.json" #optional
#displayURL="https://change.me.to.your.mods.homepage.example.invalid/" #optional
logoFile="oceans_delight_logo.png" #optional
credits=""
authors="Scouter456"
description='''
This is a mod that adds food based on the creatures in the ocean
'''
[[dependencies.examplemod]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[43,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.examplemod]]
    modId="minecraft"
    mandatory=true
# This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="[1.19.2,1.20)"
    ordering="NONE"
    side="BOTH"
